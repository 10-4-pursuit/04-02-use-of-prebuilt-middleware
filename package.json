{
  "name": "04-02-prebuilt-middleearth",
  "version": "1.0.0",
  "description": "#### Objective Create an Express server that demonstrates the use of middleware for handling CORS (Cross-Origin Resource Sharing) and parsing JSON payloads in incoming requests.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server"
  },
  "keywords": [],
  "author": "will do",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "nodemon": "^3.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
